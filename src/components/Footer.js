import React from 'react';
import { Alert } from 'reactstrap';

const Footer = (props) => {
  return (
    <div>
      <Alert color="primary">
        Developed by Mogli
      </Alert>
    </div>
  );
};

export default Footer;
